[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "openagentio"
version = "0.2.3"
description = "Python SDK for OpenAgentIO — Bridgeable runtime bus connecting MCP tools, Matrix events, and streaming agents."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [
  { name = "ModulationAI" },
]
keywords = ["agent", "bus", "nats", "acp", "asyncio", "mcp", "matrix", "bridge"]
dependencies = [
  "nats-py>=2.6",
]

[project.optional-dependencies]
validation = ["jsonschema>=4.21"]
http = ["starlette>=0.37"]
otel = ["opentelemetry-api>=1.20", "opentelemetry-sdk>=1.20"]
bridge = ["httpx>=0.27", "pyyaml>=6"]
mcp = ["mcp>=1.27.0,<2", "jsonschema>=4.21"]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "jsonschema>=4.21",
  "starlette>=0.37",
  "httpx>=0.27",
  "opentelemetry-api>=1.20",
  "opentelemetry-sdk>=1.20",
  "pyyaml>=6",
  "mcp>=1.27.0,<2",
]

[project.urls]
Homepage = "https://github.com/ModulationAI/openagentio"
Repository = "https://github.com/ModulationAI/openagentio"

[tool.hatch.build.targets.wheel]
packages = ["src/openagentio"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-ra"
